export default function AuthPage() {
  return (
    <main className="mx-auto flex max-w-2xl flex-col gap-4 py-16 px-6">
      <h1 className="text-3xl font-semibold">Authentication Disabled</h1>
      <p className="text-muted-foreground">
        The authentication flow is turned off while we focus on the static
        experience. You can still explore the rest of the site without signing
        in.
      </p>
    </main>
  );
}
